import Layout from '../common/Layout';

const CalendarPage = () => {
    return (
        <Layout showSidebar showFunctions>
            calendar page
        </Layout>
    );
};

export default CalendarPage;