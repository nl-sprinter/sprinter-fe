import Layout from '../common/Layout';

const SprintPage = () => {
    return (
        <Layout showSidebar showFunctions>
            sprint page
        </Layout>
    );
};

export default SprintPage;